// plugins/forward.js - Auto Forward stub
// handleAutoForward is called from index.js on every message

async function handleAutoForward(conn, mek, sessionId) {
    // Auto forward feature - disabled by default
    // Customize here if needed
}

module.exports = { handleAutoForward };
